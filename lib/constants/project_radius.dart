
import 'package:flutter/material.dart';

final class ProjectRadius extends Radius {

  const ProjectRadius.circularSmall() : super.circular(10);
  const ProjectRadius.circularNormal() : super.circular(20);
  const ProjectRadius.circularLarge() : super.circular(30);
  const ProjectRadius.customCircular() : super.circular(24);
  
}

